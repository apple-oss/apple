1.c函数验证线程如何被创建，主线程要pthread_join(tidp,NULL);即父线程要等待子线程的结束
，验证可得父线程和子线程的进程号相同，说明他们属于同一线程。编译程序需要在最后面加上
-lpthread。
线程无主次之分。
每个线程私有的有：栈、PC寄存器、通用寄存器；私有的有：Data段、BSS段、TEXT段、HEAP段
4-1.c通过验证函数里面的局部变量不能超过8兆，并且只能在此函数中使用，传出去会发生段错误
test5.c验证了线程号其实是栈分配的首地址
线程中创建者被被创建者有两种关系，第一种是同步关系，创建进程会等待被创建线程结束才会终止

